compile_error!(
    "Function Agent Python binding source has not been staged. Use `just \
     with-function-agent-core <command>` locally or the vendor-function-agent-core action in \
     trusted CI."
);
